# Given two string parameters, word and char, implement the function so that it
# returns a list comprised of all the indexes where the parameter char can be
# found in the parameter word.
def find_char(word, char):
    pass
